#include <stdio.h>

int main() {

    int x = -1;
    unsigned y = 0;

    if (x < y) {
        printf("x < y");
    } else {
        printf("x > y");
    }

    // write(1, "hello, world\n", 13);
    // _exit(0);
}